#!/bin/bash

# here in this script we learned about 'echo` to print a message of your choice,declares variables and assigns values to them,two variables (numbers) as input and prints their sum using those variables
# least three different built-in variables to display relevant information.
# wildcards to list all the files with a specific extension in a directory.
